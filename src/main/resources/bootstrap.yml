spring:
  application:
    name: micro-service
  profiles:
    active: ${PROFILES:native}
  cloud:
    config:
      uri: ${CONFIG_SERVER:http://127.0.0.1:8888}
      fail-fast: true


service:
  general:
      redis-open: true

